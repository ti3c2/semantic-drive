## Dev environment tips
- We are deploying to docker containers, so no need to apply local installs.
- After fixing bugs, add corresponding tests and make sure they pass.
- After implementing a feature, write a test for it.
- After you are done with editing, run formatting via ./scripts/format.sh and write a short commit message summarizing the changes done so far.