{
	local_certs
	default_sni semanticdrive.localhost
}

https://semanticdrive.localhost {
	tls internal
	encode zstd gzip

	@backend path /api/* /health /docs* /redoc* /openapi.json /s/* /embed/*
	reverse_proxy @backend backend:8000

	reverse_proxy frontend:4321
}
